# Drift Detector Test 2

A test project used to validate the Codebase Drift Detector, including OpenAPI and ADR fetch paths.

## API

### Get User

Retrieves a user by their ID.

**Endpoint:**

```http
GET /users/{id}
```
